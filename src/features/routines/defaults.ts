/**
 * Name a routine is born with. Shared so the screen can recognise a routine the user
 * never actually touched and clean it up rather than littering the list.
 */
export const DEFAULT_ROUTINE_NAME = 'New Routine';

export const DEFAULT_TARGET_SETS = 3;
